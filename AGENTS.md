# AGENTS.md

이 파일은 이 저장소에서 작업하는 ChatGPT Work, Codex 및 다른 자동화 에이전트가 반드시 따라야 하는 규칙입니다. 하위 폴더에 더 구체적인 `AGENTS.md`가 있으면 해당 범위에서는 하위 규칙도 함께 적용합니다.

## 1. 기준 저장소

- GitHub를 프로젝트 소스와 이력의 단일 기준점으로 사용한다.
- GitHub 원격이 연결된 뒤에는 원격 `main`보다 오래된 소스로 작업하지 않는다.
- Sites 배포본, 로컬 파일, 대화 기록만을 최신 소스라고 가정하지 않는다.
- 현재 GitHub 원격이 아직 준비되지 않았다면 임의 저장소를 만들거나 다른 저장소에 Push하지 않고 사용자 승인을 기다린다.

## 2. 작업 시작 절차

1. `git status`, 현재 브랜치, 원격 주소를 확인한다.
2. 사용자 소유의 미커밋 변경을 덮어쓰거나 삭제하지 않는다.
3. GitHub 원격이 연결되어 있으면 최신 원격 상태를 확인하고 동기화한다.
4. 변경 목적에 맞는 작업 브랜치를 사용한다. `main`에서 직접 기능을 개발하지 않는다.
5. `README.md`, `PROJECT.md`, `CURRENT_STATUS.md`, `AI_MEMORY.md`와 관련 코드를 읽고 현재 정책을 확인한다.

동기화 과정에서 충돌, 로컬 변경, 원격 분기처럼 사용자 판단이 필요한 상황이 발생하면 임의 해결하지 말고 보고한다.

## 3. 구현 원칙

- 기존 기능, 데이터, API, DB 필드, 관리자 메뉴를 임의 삭제하지 않는다.
- 사용자 승인 없이 폴더 구조, 핵심 아키텍처, 인증 방식, DB 구조, 결제 및 리워드 정책을 크게 변경하지 않는다.
- 요청 범위 밖의 대규모 리팩터링이나 라이브러리 교체를 하지 않는다.
- 기존 패키지 관리자와 `package-lock.json`을 유지한다.
- 새 의존성은 필요성과 유지보수 위험을 검토한 뒤 최소한으로 추가한다.
- 화면만이 아니라 서버 권한, 입력 검증, DB 일관성, 모바일 동작을 함께 고려한다.
- 기존 코드에 한글 인코딩 손상이 있으면 범위를 확인하고 별도 승인 없이 광범위한 일괄 치환을 하지 않는다.

## 4. 상품 등록 규칙

- 상품 등록과 정보 확인은 반드시 품번(`style_number`) 검색을 기준으로 진행한다.
- 상품 코드(`product_code`)와 품번을 혼용하지 않는다.
- 동일 품번의 중복 상품을 만들지 않는다.
- 품번, 브랜드, 상품군, 공식 상품명, 색상 및 주요 사양을 여러 신뢰 가능한 출처로 교차 확인한다.
- 상품 이미지는 최대 12장만 등록한다.
- 이미지가 실제 품번과 일치하는지, 중복·저해상도·깨진 링크가 없는지 확인한다.
- 검수된 이미지는 가능한 한 로컬 정적 자산 또는 R2에 보존한다.
- 확인되지 않은 내용을 사실처럼 만들어 넣지 않는다.
- 초안 상품을 임의로 공개하지 않는다.

## 5. DB와 보안

- DB 변경은 `db/schema.ts`와 새 Drizzle 마이그레이션으로 관리한다.
- 적용된 기존 마이그레이션 파일을 재작성하지 않는다.
- 운영 DB를 직접 초기화하거나 삭제하지 않는다.
- `.env`, API 키, 비밀번호, 세션, 고객 개인정보를 커밋하거나 문서에 기록하지 않는다.
- 관리자 초기 자격 증명이나 운영 비밀값을 응답에 노출하지 않는다.
- 인증, 관리자 권한, 포인트 원장, 주문 결제 상태 변경에는 관련 테스트가 필요하다.

## 6. 검증

- 변경 범위와 관련된 테스트를 우선 실행한다.
- GitHub에 올리기 전 전체 빌드를 실행한다.
- DB, 주문, 인증, 리워드처럼 위험도가 높은 변경은 통합 테스트까지 확인한다.
- 테스트를 실행할 수 없는 환경이면 성공했다고 말하지 않고 이유와 미검증 범위를 기록한다.
- 배포는 사용자가 명시적으로 승인한 경우에만 진행한다.

## 7. 작업 종료 절차

1. `git diff`와 `git status`로 변경 범위를 검토한다.
2. 실제 변경 내용을 `CHANGELOG.md`에 기록한다.
3. 완료·진행·미완료·버그·다음 작업 상태를 `CURRENT_STATUS.md`에 반영한다.
4. 정책이나 구조가 바뀌면 `PROJECT.md`, 장기 운영 방향이 바뀌면 `AI_MEMORY.md`도 갱신한다.
5. 생성·수정 파일, 실행한 검증, 남은 위험을 사용자에게 보고한다.
6. 사용자 승인 없이 커밋, Push, Pull Request, 배포를 진행하지 않는다. 단, 사용자가 해당 동작을 명시적으로 요청한 경우에는 그 범위 안에서 진행한다.

## 8. 문서 유지

- 프로젝트 문서는 코드와 같은 변경에서 함께 최신화한다.
- 아직 구현되지 않은 기능을 완료로 기록하지 않는다.
- 코드에서 확인한 사실과 운영자가 결정해야 할 정책을 구분한다.
- 일시적인 대화 내용보다 저장소 문서를 우선하되, 사용자 최신 지시가 문서와 충돌하면 작업을 멈추고 확인한다.

## 9. 사용자 설명 방식

- 사용자는 개발 초보자를 위한 쉬운 설명을 원한다.
- 영어 용어는 `GitHub(깃허브: 소스와 작업 기록을 보관하는 서비스)`처럼 한국식 발음과 짧은 뜻을 함께 적는다.
- 같은 답변에서 반복되는 용어는 처음 등장할 때만 풀이해도 된다.
- 사용자가 용어 풀이를 그만해도 된다고 말할 때까지 이 방식을 유지한다.
