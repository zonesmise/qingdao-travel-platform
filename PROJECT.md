# 프로젝트 정의

## 상품 분류와 브랜드 원칙

- 브랜드는 이름이 같으면 하나의 기준 항목으로 관리한다.
- 한 브랜드는 신발·의류·가방·지갑·벨트·액세서리 등 여러 상품 대분류에 연결할 수 있다.
- 상품은 공식 대분류·하위 분류와 브랜드를 각각 가진다.
- 상품 등록 화면은 선택한 대분류에 연결된 브랜드를 우선 제공하되, 기존 상품의 값을 임의로 삭제하지 않는다.
- 채널은 전체 상품 DB(디비: 데이터 저장소)에서 검색·브랜드·공식 분류 조건으로 상품을 골라 자체 카테고리에 배치한다.
- 같은 상품을 같은 채널 카테고리에 반복 선택해도 상품 ID 기준으로 한 번만 연결한다.
- 채널의 일반 상품 메뉴는 공식 분류 조건으로, 브랜드 하위 메뉴는 브랜드 조건으로 자동 구성한다.
- 추천상품·한정판·협업처럼 운영 판단이 필요한 진열 메뉴만 상품을 직접 선택한다.

## 회원 채널 관리 원칙

- 최고 관리자와 채널 회원 화면은 같은 채널 편집 컴포넌트를 재사용한다. 화면을 별도로 복제하지 않고 권한 차이는 서버에서 제한한다.

- 회원 채널 관리 화면은 우선 최고 관리자 라이브 커머스와 같은 메뉴·작업 흐름을 제공하고, 실제 운영 검토 후 불필요하거나 위험한 메뉴를 단계적으로 숨긴다.
- 생방송·방송 상품 번호표·다시보기 상품 시간표·쇼츠·상담·보조창은 채널 소유자가 자기 채널에서 관리한다.
- 가격·재고·주문·정산·최종 공개와 다른 채널 자료는 회원 채널에서 변경할 수 없다.

## 상담 운영 구조

- 전체 홈페이지는 쇼핑몰 공통 상담 담당자와 상담 채널을 사용한다.
- 각 라이브 커머스 판매 채널은 채널 기본정보에서 전용 상담 담당자와 카카오톡·텔레그램·라인·실시간 상담 주소를 별도로 설정할 수 있다.
- 채널 전용 상담을 사용하지 않는 채널은 쇼핑몰 공통 상담 설정을 이어받는다.

## 채널 중심 홈페이지 구조

홈페이지는 공통 쇼핑몰 기능 위에 여러 방송 판매 채널을 모으는 플랫폼 구조를 사용합니다. 첫 화면의 우선순위는 `오늘의 HOT 채널`, 채널별 라이브·재방송·쇼츠, 채널 추천 상품, 전체 상품 순서입니다. 각 채널은 독립적인 대표 이미지와 색상, 운영자, 방송 정보, 연결 상품을 가지며 기존 주문과 리워드 기능을 공동으로 사용합니다.


## 프로젝트 목적

리워드 쇼핑몰 V2는 상품 판매와 회원 리워드 운영을 하나의 서비스에서 관리하는 장기 운영형 쇼핑몰입니다. 고객은 활동과 구매를 통해 리워드를 적립하고 상품 결제에 사용할 수 있으며, 운영자는 상품·주문·회원·정책·라이브 커머스를 관리자 화면에서 통합 관리합니다.

이 프로젝트의 소스 기준점은 GitHub로 전환합니다. ChatGPT Work와 Codex는 같은 GitHub 이력과 프로젝트 문서를 기준으로 협업합니다.

## 기술 스택

| 영역 | 기술 |
| --- | --- |
| UI | React 19, Next.js 16 호환 App Router, Tailwind CSS 4 |
| 런타임/빌드 | Vinext, Vite 8, Cloudflare Workers |
| 언어 | TypeScript 5 |
| 데이터 | Cloudflare D1 (SQLite), Drizzle ORM/Kit |
| 파일 저장 | Cloudflare R2 |
| 인증 | 자체 회원 세션, Google ID 토큰, 관리자 세션 |
| AI | Gemini 기반 유튜브 재방송 분석(설정 시) |
| 테스트 | Node test runner 기반 회귀·통합 테스트 |
| 호스팅 | OpenAI Sites |
| 소스 관리 목표 | GitHub, `main` + 작업 브랜치 + Pull Request |

## 폴더 구조

| 경로 | 역할 |
| --- | --- |
| `app/` | 고객/관리자 페이지, API, 메타데이터 |
| `components/` | 스토어, 상품 상세, 인증, 관리자 대시보드, 라이브 및 상담 UI |
| `db/` | Drizzle 스키마와 D1 바인딩 |
| `drizzle/` | 버전 관리되는 SQL 마이그레이션 |
| `lib/` | 데이터 초기화, 회원/관리자 인증, 권한, 포인트, 결제, 상품 정책 |
| `public/` | 로고, 정적 리소스, 검수된 상품 이미지 |
| `scripts/` | CI 설치/빌드 검증, 공급처 미디어 및 카탈로그 생성 |
| `tests/` | 인증, 결제, 리워드, 관리자, 상품, 모바일 UI 회귀 테스트 |
| `worker/` | D1/R2/환경 변수와 앱을 연결하는 Worker 진입점 |
| `.openai/` | Sites 프로젝트 ID와 D1/R2 논리 바인딩 |
| `build/` | 현재는 Sites Vite 플러그인 소스 포함. 통째로 산출물 취급하지 않음 |
| `generated/` | 카탈로그 생성 결과 JSON |

## DB 구조

현재 `db/schema.ts` 기준 30개 테이블을 사용합니다.

| 도메인 | 테이블 |
| --- | --- |
| 환경 설정 | `settings` |
| 회원/인증 | `members`, `member_credentials`, `member_identities`, `member_sessions`, `password_reset_requests`, `test_data_members` |
| 상품/구매 | `products`, `product_catalog_details`, `carts`, `wishlists`, `shipping_addresses`, `orders`, `order_items` |
| 리워드/추천 | `point_logs`, `referral_codes`, `referral_visits`, `referrals`, `reward_events`, `referral_flags`, `attendance_records` |
| 운영/고객지원 | `finance_requests`, `reviews`, `popups`, `notices`, `coupons`, `inquiries`, `audit_logs` |
| 관리자 | `admin_accounts`, `admin_sessions` |

DB 변경 규칙:

1. `db/schema.ts`를 수정합니다.
2. `npm run db:generate`로 마이그레이션을 생성합니다.
3. 생성 SQL과 기존 데이터 호환성을 검토합니다.
4. 관련 테스트와 전체 빌드를 통과시킵니다.
5. DB 문서와 변경 이력을 함께 갱신합니다.

운영 DB를 직접 수정하거나 기존 마이그레이션을 재작성하지 않습니다.

## 회원 구조

- 회원 기본 역할은 `member`, 기본 상태는 `active`입니다.
- 신규 회원의 포인트 잔액은 0에서 시작합니다.
- 이메일/비밀번호 가입 시 이용약관과 개인정보 처리 동의가 필요합니다.
- 이메일/비밀번호 가입 시 비밀번호 재입력과 국내 휴대전화 번호 형식을 화면과 서버에서 함께 검증합니다.
- 상품에서 시작한 로그인·회원가입은 안전한 내부 복귀 주소를 유지해 원래 구매 흐름으로 돌아갑니다.
- Google 계정 가입·로그인을 지원하며 Google 계정 식별자를 별도 연결합니다.
- 회원 세션은 해시로 저장하며 기본 유지 기간은 30일입니다.
- 로그인 실패는 누적 관리하며 반복 실패 시 일시 잠금합니다.
- 회원 포인트는 총 잔액과 충전 포인트, 리워드 포인트, 지급 대기 리워드를 구분합니다.
- 비밀번호 찾기는 현재 관리자 처리용 요청을 생성하는 방식입니다.
- 이메일/휴대폰 인증 상태 필드는 존재하지만 SMS 인증 서비스는 아직 연결되지 않았습니다.

## 포인트 정책

- 사용자 화면 용어는 “리워드”를 기본으로 사용합니다.
- 회원 잔액은 `charge_points`와 `reward_points`로 분리하며 거래는 `point_logs`에 기록합니다.
- 사용 시 사용 가능한 리워드를 우선 배분하고 부족분을 충전 포인트에서 사용합니다.
- 주문 결제 대기 중인 포인트는 예약하고, 입금 확인 시 차감 확정하며, 취소/만료 시 복원합니다.
- 상품별로 리워드 사용 불가, 일부 비율, 전액 사용 정책을 설정할 수 있습니다.
- 구매 리워드는 상품 정책과 현금 결제 대상 금액, 회원 등급 가산율을 반영합니다.
- 기본 후기 리워드는 텍스트 300, 사진 500이며 관리자 설정으로 변경할 수 있습니다.
- 기본 출석 리워드는 일 100, 7일 연속 보너스 500입니다.
- 기본 추천 정책은 가입 500, 첫 주문 추천인 1,000, 친구 1,000, 최소 주문 30,000, 보류 7일입니다.
- 추천 리워드는 월 한도와 만료일을 가지며, 해당 주문이 취소·반품되면 회수할 수 있어야 합니다.
- 모든 지급/사용/취소는 중복 처리 방지와 원장 일관성을 유지해야 합니다.

위 숫자는 코드의 현재 기본값이며 운영자가 설정 화면에서 변경할 수 있습니다. 코드 기본값을 바꿀 때는 운영 DB 설정과의 차이를 반드시 확인합니다.

## 상품 정책

- 상품 등록과 검수의 기준 키는 상품명이나 임의 코드가 아니라 `style_number`(품번)입니다.
- 품번은 비어 있지 않은 경우 DB에서 중복될 수 없습니다.
- `product_code`와 `style_number`는 서로 다른 필드로 유지합니다.
- 상품군은 신발, 의류, 가방, 벨트, 지갑, 기타 액세서리를 지원합니다.
- 상품군별 상세 필드는 `product_catalog_details`와 JSON 필드로 확장합니다.
- 공개 상품은 활성 상태의 상품만 노출합니다. 관리자 미리보기는 비활성/초안 상품을 확인할 수 있지만 구매 기능은 잠급니다.
- 상품 이미지는 품번 검색과 출처 검수를 거쳐 등록합니다.
- 상품당 이미지는 최대 12장입니다.
- 외부 URL 의존보다 검수된 로컬 정적 자산 또는 R2 저장을 우선합니다.
- 이미지 출처, 품번 일치 여부, 대표 이미지, 중복, 해상도를 확인합니다.
- 카테고리에 상품이 남아 있으면 카테고리를 임의 삭제하지 않고 먼저 상품을 이동합니다.
- 옵션/변형별 SKU, 추가 가격, 재고 정책을 지원합니다.

## 관리자 정책

- 관리자 역할은 `supervisor`와 제한된 운영 계정으로 구분합니다.
- 기능별 권한 키를 서버에서 검증하며 UI 숨김만으로 권한을 대체하지 않습니다.
- 관리자 세션과 회원 세션은 분리합니다.
- 상품, 주문, 회원, 리워드, 후기, 문의, 팝업, 공지, 카테고리, 라이브 커머스 및 운영 설정을 관리합니다.
- 중요한 관리자 작업은 `audit_logs`에 기록합니다.
- 초기 관리자 비밀번호는 운영 전에 반드시 교체하고 저장소나 문서에 기록하지 않습니다.
- 권한, 인증, 결제 확정, 리워드 조정 로직 변경은 사용자 승인과 관련 테스트가 필요합니다.

## 현재 개발 규칙

1. GitHub를 소스의 단일 기준점으로 사용합니다.
2. 작업 시작 전에 원격 최신 상태와 현재 브랜치, 변경 파일을 확인합니다.
3. 기능별 작업 브랜치를 사용하고 `main`에 직접 개발하지 않습니다.
4. 기존 기능, DB 컬럼, API, 정책을 사용자 승인 없이 삭제하거나 의미를 바꾸지 않습니다.
5. 구조, 인증, DB, 결제, 리워드 정책의 큰 변경은 구현 전에 사용자 승인을 받습니다.
6. 변경에 맞는 테스트를 추가하거나 갱신하고 빌드로 검증합니다.
7. `CHANGELOG.md`, `CURRENT_STATUS.md`, 필요 시 `PROJECT.md`와 `AI_MEMORY.md`를 함께 갱신합니다.
8. 실제 비밀값, 고객 개인정보, 운영 DB 데이터는 GitHub에 올리지 않습니다.
9. 배포는 별도 승인 작업이며 코드 변경 또는 GitHub Push가 자동 배포 승인을 뜻하지 않습니다.
10. 장기 유지보수성과 데이터 보존을 빠른 임시 수정보다 우선합니다.

## GitHub 저장소 운영

- 저장소: `zonesmise/reward-point-mall-v2`
- 공개 범위: Private
- 기본 브랜치: `main`
- 작업 브랜치 예시: `feat/...`, `fix/...`, `docs/...`, `chore/...`
- 병합 방식: Pull Request 검토 후 병합
- 보호 권장: `main` 직접 Push 제한, 빌드/테스트 성공 요구
- 대용량 이미지: 현재 `public/`에 약 53MB의 상품 자산이 있으므로 GitHub 제한을 넘는 단일 파일이 없는지 확인하고, 증가 추세에 따라 Git LFS 또는 R2 중심 운영을 검토합니다.
- 기존 Sites 내부 원격은 `sites`로 보존하고 GitHub를 `origin`으로 지정합니다.

GitHub Push와 배포는 각각 사용자 요청 범위 안에서만 진행합니다.
## 판매 채널과 리워드 확장 원칙

- 기존 구매자 포인트 계산, 적립, 취소·반품 회수 정책은 유지합니다.
- 판매 채널은 사람과 방송, 상품을 묶는 독립된 운영 단위입니다.
- 주문에는 유입 판매 채널을 기록하되, 1차 단계에서는 채널 운영자 정산을 계산하지 않습니다.
- 채널과 상품은 다대다 관계로 연결하며 같은 상품을 여러 채널에서 소개할 수 있습니다.
- 공개 채널만 고객 화면에 노출하고 새 채널은 기본적으로 초안으로 저장합니다.
- 향후 정산 기능은 구매자 리워드 원장과 분리된 원장으로 설계합니다.
## 판매 채널 공개 화면과 귀속 규칙

- 공개 채널 주소는 `/channel/[slug]`를 사용합니다.
- 채널 화면은 생방송, 다시보기, 쇼츠, 채널 상품과 다른 채널 탐색 영역으로 구성합니다.
- 상품 원본과 재고는 전체 쇼핑몰에서 한 번만 관리하고 여러 채널에 연결할 수 있습니다.
- 채널 상품 메뉴에는 연결 상품이 존재하는 카테고리만 자동 노출합니다.
- 상품 원본·가격·재고·기본 분류는 전체 쇼핑몰에서 한 번만 관리하고, 채널 운영자는 전체 상품에서 판매할 상품을 연결합니다. 채널은 상품 원본을 복제하거나 기본 분류를 바꾸지 않고 고객에게 표시할 카테고리 이름·노출·순서만 별도로 관리합니다.
- 채널 운영자는 고객에게 보일 채널 전용 카테고리를 먼저 만들고, 각 카테고리에 전체 상품을 검색·필터·일괄 선택하여 연결합니다. 한 상품은 여러 채널 카테고리에 속할 수 있고 기존 채널 상품 중 아직 배치하지 않은 상품은 미분류로 보존합니다.
- 전체 상품 DB는 채널의 `상품·카테고리`에서 상품을 가져올 때만 검색합니다. 생방송 번호표·다시보기 상품 시간표·쇼츠에서는 현재 채널에 가져온 상품만 대상으로 하며, 채널 카테고리·브랜드·상품명·품번·상품코드 필터와 페이지 이동으로 선택합니다.
- 고객용 홈과 판매 채널은 전체 상품 상세정보를 한 번에 브라우저로 보내지 않습니다. 서버의 경량 상품 색인으로 검색·카테고리 숫자를 계산하고, 화면에는 현재 페이지 상품 8개와 방송 콘텐츠에 직접 연결된 상품만 전달합니다.
- 관리자 화면은 선택한 메뉴 범위만 조회하며, 상품 편집 메뉴가 아닌 방송·운영 메뉴에서는 필요한 상품 요약 필드만 사용합니다.
- 채널 카테고리는 관리 복잡도를 낮추기 위해 대메뉴와 2단계 하위 메뉴까지만 사용합니다. 브랜드·상품 유형처럼 하위 구분이 필요한 항목은 대메뉴 아래 2단계로 구성합니다.
- 채널별로 카테고리 표시 이름·노출 여부·순서를 따로 저장하되 상품의 원래 분류는 변경하지 않습니다.
- 다른 채널은 현재 채널의 핵심 콘텐츠 뒤, 페이지 하단에 노출합니다.
- 다른 채널 추천 목록은 최고 관리자가 노출 여부와 순서를 정하며 무작위로 섞지 않습니다. 현재 채널은 자동 제외하고, 채널이 많으면 가로 이동으로 탐색합니다.
- 고객 채널 화면은 별도 축소 화면을 만들지 않고 기존 오르미르의 생방송·채팅·현재 상품·이전 상품·다시보기 시간표·쇼츠·상품 기능 전체를 채널별로 재사용합니다.
- 관리자 화면은 관리 채널을 먼저 선택한 뒤 기존 방송 설정·다시보기·쇼츠·상품 번호표·방송 보조창을 같은 방식으로 사용합니다.
- 기존 단일 방송 설정은 첫 번째 기본 채널로 안전하게 이전하고, 이전이 끝난 뒤에도 기존 경로의 호환성을 유지합니다.
- 현재 채널은 추천 목록에서 제외하며 다른 공개 채널만 표시합니다.
- 판매 귀속은 주문 전체가 아니라 주문 상품 단위가 기준입니다.
- 채널에서 상품을 장바구니에 담을 때 해당 상품의 채널 귀속을 저장합니다.
- 여러 채널 상품이 한 주문에 포함되면 각 주문 상품의 채널 귀속을 각각 유지합니다.
- 교차 채널 소개 보상과 방송자 정산은 운영 정책이 확정될 때 별도 원장으로 추가합니다.
# 전체 상품 분류 정책

- 전체 쇼핑몰의 공식 상품 분류는 최대 3단계(`대분류 → 중분류 → 소분류`)까지 허용합니다.
- 모든 분류가 반드시 3단계를 가져야 하는 것은 아니며 1단계 또는 2단계에서 끝낼 수 있습니다.
- 상품은 대분류와 최종 선택 분류를 저장하며 중분류는 소분류를 묶는 구조로 사용합니다.
- 브랜드는 상품 분류와 분리하여 별도로 관리합니다.
- 추천상품, 신상품, 한정판 같은 판매 강조 항목은 공식 상품 분류가 아니라 진열 정책으로 관리합니다.
- 채널 전용 표시 카테고리는 전체 상품 공식 분류와 별개이며 최대 2단계를 유지합니다.
<!-- 채널 대표 이미지는 R2에 가로 카드용 image_url, 정사각형 로고용 avatar_image_url, 재편집용 original_image_url을 별도로 저장하며, 이전 데이터는 image_url로 대체 표시합니다. -->
# 회원 방송 채널 권한 구조

- 회원: 채널 개설 신청, 승인 후 자기 채널 기본정보와 방송 내용 작성, 공개 검수 요청
- 최고 관리자: 신청 승인·수정 요청·반려, 자료 보완, 최종 공개·공개 중지
- 공개 전 상태: 신청 검토 중 → 운영 준비 중 → 공개 승인 대기 → 공개 중
- 마이페이지는 방송 채널 신청과 승인 전 상태 확인만 담당하며, 승인된 채널 소유자는 자기 채널 상단의 `내 채널 관리`에서 별도 운영 화면으로 이동합니다.
- 승인 후에도 마이페이지의 채널 메뉴는 `채널 관리`로 유지하되 기본정보 요약만 보여주고, 실제 편집은 고객 채널 상단을 유지하는 큰 전용 관리 화면에서 진행합니다.
- 최고 관리자 화면은 `라이브 방송 신청 상황`과 `라이브 방송 채널 관리`를 분리하고, 승인 전 신청은 신청 상황에서만, 승인된 채널은 채널 관리에서만 다룹니다.
- 보안 원칙: 회원용 API는 `owner_member_id`로 소유권을 검사하고 전체 관리자 API를 노출하지 않는다.
- 채널 소유자 관리 범위: 채널 기본정보, 생방송, 전체 상품에서 채널 상품 선택, 채널 카테고리, 다시보기, 쇼츠와 연결 상품, 채널 전용 상담
- 최고 관리자가 채널을 직접 추가할 때는 가입 회원아이디를 검색해 채널 소유자를 반드시 지정하며, 한 회원은 한 채널만 운영합니다.
- 최고 관리자 전용 범위: 상품 원본·가격·재고, 주문·결제·정산, 채널 신청 승인과 최종 공개·중지
## 채널 소유자 관리 권한

- 채널 소유자는 `/my-channel`에서 자신의 채널만 관리합니다.
- 상품 원본·가격·재고·주문·정산·최종 공개는 최고 관리자 권한입니다.
- 채널 카테고리는 전체 쇼핑몰 상품 분류와 충돌하지 않도록 채널 소유자의 변경 요청과 최고 관리자 승인을 거쳐 반영합니다.
- 모바일은 채널 확인과 간편 운영 중심으로 제공하고, 생방송 주소·상품·영상의 복잡한 편집은 PC에서만 제공합니다.
## 채널 브랜드 자동 연결 규칙

- 채널의 브랜드 메뉴는 브랜드명만으로 상품을 연결하지 않습니다.
- `상품군 + 브랜드`가 모두 일치할 때만 자동 연결합니다. 예: `의류 + 나이키 (Nike)`.
- 상품군을 지정하지 않은 브랜드 메뉴는 다른 상품군 혼입을 막기 위해 상품을 자동 연결하지 않습니다.
- 같은 브랜드를 신발과 의류에서 판매하려면 각 채널 또는 채널 카테고리에서 상품군을 별도로 지정합니다.
# 해외직구 주문 정책

- 판매 상품은 중국 판매자가 공급하며 플랫폼은 한국 고객의 결제, 배송 추적, 고객지원, 반품과 환불 창구를 책임진다.
- 주문 전에 중국 출고 상품, 예상 배송기간, 통관 지연 가능성, 개인통관고유부호 필요 여부와 반품 비용 기준을 명확히 표시한다.
- 개인통관고유부호 원문은 암호화 저장하고 화면과 일반 API에는 마스킹 값만 노출한다. 주문에는 주문 당시 통관정보를 별도 보존한다.
- 배송은 결제 확인, 중국 출고 준비, 중국 물류센터, 검수, 국제 운송, 한국 도착, 통관, 국내 배송, 배송 완료 단계로 관리한다.
- 하자·오배송·파손 등 판매자 또는 플랫폼 책임 사유와 단순 변심을 구분하여 반품 비용 부담을 결정한다.
